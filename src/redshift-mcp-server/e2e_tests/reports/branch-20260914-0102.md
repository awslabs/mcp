# End-to-end test: Changes on this branch

2026-09-14 01:02 UTC

- **Scenario**: `branch`
- **Code under test**: `feat/redshift-session-redesign` at `0b92cdd4`, uncommitted changes
- **Agent**: `kiro-cli`, model `claude-opus-5`
- **Exit status**: `0`
- **Duration**: 17.9 min
- **Region**: `us-east-1`
- **Provisioned**: `mcp-e2e-provisioned`, ra3.large x2
- **Serverless**: `mcp-e2e-serverless`, 8 RPU
- **Sample data**: `tickit` in `dev`, 424,319 rows each

## Summary

| Scenario | Result | Comment |
|---|---|---|
| Read succeeds when BatchExecuteStatement is denied (`nb_*`, both warehouses) | PASS | Falls back within the same call; bare statement id confirms `ExecuteStatement`, not a batch |
| Fallback latches after one denial and does not re-pay it per statement | PASS | One latch logged, then 17 statements on the compatibility path with no further denial |
| Batch path re-probed after the 300s window without a restart | PASS | Re-latched at 00:55:32, ~522s after the first latch; the read still returned rows |
| Read-only wrapper dropped on the fallback path | PASS | `BEGIN READ ONLY` absent from the denied server's log after latching |
| Write refused under denied batch, naming the grant (`nb_*`) | PASS | |
| Write refused under denied batch with confirmation skipped (`nbw_*`) | PASS | Reaches the fallback's own refusal, which a confirming configuration cannot surface |
| `SET` under `nbw_*` refused by the fallback, not the read-only deny-list | PASS | Confirms the guard stands down in read-write mode and the fallback declines |
| Named transactions refused under denied batch, and the name never opens | PASS | Both `begin_transaction` and `in_transaction`, on both denied configurations |
| Read-only guard still runs ahead of the fallback | PASS | `GRANT` and multi-statement rejected by the guard, not the fallback |
| Discovery tools work under denied batch | PASS | databases, schemas, tables, columns; `list_clusters` unaffected as it avoids the Data API |
| `review_cluster` works under denied batch | PASS | All 11 diagnostic queries ran, 10 findings |
| Non-denied configurations never use the fallback | PASS | Zero latch events in all three logs; every statement carries a `:N` batch sub-statement id |
| Read-only deny-list refusals (`ro_*`) | PASS | UNLOAD, VACUUM, SET, RESET, TRUNCATE, BEGIN, ABORT, GRANT, COMMIT-in-transaction |
| Deny-listed words as identifier, alias or literal are allowed | PASS | No false positives; `EXPLAIN` and `SHOW` allowed as reads |
| Multi-statement, unparseable and nested-comment input rejected | PASS | Nested-comment prefix rejected as unparseable, so nothing is smuggled |
| Engine backstop refuses non-enumerated writes in read-only mode | PASS | `CREATE TABLE` and `INSERT` both hit "transaction is read-only"; nothing persisted |
| Read-only named transaction lifecycle | PASS | Open with first statement, add statements bare on the session, close with a last statement |
| Guard rejection inside a transaction leaves it open | PASS | |
| Failed statement rolls the transaction back and drops the name | PASS | |
| Reopening an open name refused; closed name reported gone | PASS | |
| Transaction cap of 10 per target, counted per target | PASS | 11th refused naming `MAX_OPEN_TRANSACTIONS_PER_TARGET`; a second warehouse still accepted one |
| Transaction bound to its own server process | PASS | A name opened on `ro_*` is unknown to `rw_*` |
| Transaction parameter validation | PASS | Two parameters refused naming both; blank name refused; missing `sql` refused |
| Write fails closed when the client cannot be prompted (`rw_*`) | PASS | Refusal names `UNSAFE_SKIP_WRITE_CONFIRMATION`; nothing created |
| Reads and multi-statement rejection unchanged in read-write mode | PASS | `:2` sub-statement id confirms the read-only wrapper is absent |
| Unconfirmed writes execute with autocommit (`rwu_*`, both warehouses) | PASS | |
| Read-write transaction: rollback discards, commit persists | PASS | |
| Anticipated failures reach the model as actionable messages | PASS | Unknown cluster on `execute_query` and `review_cluster`; engine errors quoted verbatim |
| Seeded data and schema left intact | PASS | Row counts unchanged, no probe tables remain on either warehouse |

- `rw_*` never completes a confirmation round trip: this CLI does not advertise MCP elicitation, so only the fail-closed branch is observable. A confirm and a decline need a client that can prompt.
- The uncommitted working-tree changes are confined to the e2e harness (report summary format) and alter no server behaviour; the server code under test is the committed branch diff.
- The `nb_*` and `nbw_*` denials are genuine explicit denies in a session policy, quoted in the server logs, not simulated.
- Unit suite green: 388 passed.

## Prompt

Written by the harness, not by hand. The agent derives the cases from the package's unit tests, and is told where to look rather than what to expect.

You have the Redshift MCP server available at several configurations, each running the working tree you are testing. Every configuration exposes the same tools, distinguished by prefix, so `ro_execute_query` and `rw_execute_query` are the same tool at two configurations:
- `ro_*` — default configuration: read-only, which is what a user gets by installing it
- `rw_*` — ACCESS_MODE=read-write, so writes are permitted and each one is confirmed
- `rwu_*` — ACCESS_MODE=read-write with UNSAFE_SKIP_WRITE_CONFIRMATION=true, so writes run unconfirmed
- `nb_*` — default configuration, but its credentials are denied redshift-data:BatchExecuteStatement
- `nbw_*` — ACCESS_MODE=read-write with UNSAFE_SKIP_WRITE_CONFIRMATION=true, and its credentials are denied redshift-data:BatchExecuteStatement

Two warehouses are provisioned and seeded with the TICKIT sample schema `tickit` in database `dev`: provisioned cluster `mcp-e2e-provisioned` and Serverless workgroup `mcp-e2e-serverless`. Both hold the same tables, so a check that holds on one should hold on the other. Other clusters exist in this account; leave them alone.

Run an end-to-end test covering the scenarios for the changes introduced in the current branch only. Scope is the committed branch diff AND the uncommitted working-tree changes, so read `git status` and `git diff` as well as the branch commits: the newest behaviour may not be committed yet. Get the test scenario ideas from the unit tests under the project directory.

Some of the branch behaviour appears only when the AWS principal is denied a specific IAM action, which is what the denied configurations above are for. Work out from the diff and the unit tests what each tool should do under those credentials, and verify it. Verify too that behaviour under the configurations that are not denied it is unchanged.

End your reply with a section that begins on its own line with `## Summary` and holds nothing but,
in this order:

1. A markdown table with the columns Scenario, Result and Comment. One row per scenario. Result is
   PASS or FAIL. Leave Comment empty unless there is something to say.
2. Only if anything is worth noting, a short bulleted list under the table.

This section is lifted into a committed report and read on its own, so keep every row
intelligible without the rest of the reply.

## Transcript

I'll start by understanding what the branch changes.Now the server tool layer, particularly write confirmation.Plan is clear. Starting with baseline reads on both warehouses.Batch wrapper confirmed (`query_id` sub-statement `:3`). Now the denied-batch configurations.Fallback confirmed: no `:N` sub-statement suffix, and a distinct denied principal.Now the discovery tools and review under denied batch — these are server-authored SQL.Now the read-write configuration that is also denied the batch action (`nbw_*`).Now the configurations that are not denied the action, starting with the read-only engine backstop.Now read-only named transactions on the non-denied configuration.Parameter validation on the transaction arguments:Now the confirming read-write configuration (`rw_*`), which should fail closed on writes.Note `:2` — the read-only wrapper is absent in read-write mode. Now a write there:Now `rwu_*`, where writes run unconfirmed.Now a read-write transaction: rollback must discard, commit must persist.Scratch objects removed. Now checking the server logs for latch behaviour, which is only observable there.One live check remains: the 300s re-probe. The latch fired at 00:46:53, so a read now should re-probe.Configuration matrix confirmed (defaults for keepalive and the transaction cap). Testing the per-target cap of 10.Cap holds at 10. Verifying it is counted per target, not globally:Cleaning up the open transactions.Final verification: no residue left, and the unit suite still passes against this working tree.The branch's changes hold end to end. Two findings worth stating up front: the denied-batch fallback behaves exactly as the diff specifies, and the three configurations that are not denied the action never touch that path at all — their logs show zero latch events, and every one of their statements carries a `:N` batch sub-statement id.

What the evidence rests on. The denied configurations run as a distinct principal (`IAMR:mcp-e2e-denied-batch`) whose session policy carries an explicit deny on `redshift-data:BatchExecuteStatement`, quoted verbatim in the server log. Reads under it return a bare statement id with no `:N` suffix, which is how a single `ExecuteStatement` is distinguishable from a batch sub-statement, and `BEGIN READ ONLY` appears zero times in that server's log after the latch — the wrapper is genuinely dropped with the batch rather than being sent and ignored.

The latch and its re-probe were both observed live. The first denied read at 00:46:50 was refused at 00:46:53 and re-run on the compatibility path at 00:46:54, so the caller saw a successful read, not an error. The next 17 statements ran on that path with no further denial logged. A read issued at 00:55:32, past the 300s window, produced a fresh denial and re-latch while still returning its rows, which is the re-probe working without a restart.

The two denied configurations exist to reach different gates, and they did. Under `nb_*` a write is refused with the message naming the grant; under `nbw_*`, where confirmation is skipped, the same message surfaces for `CREATE TABLE` and for `SET` — the latter matters because `SET` is on the read-only deny-list, so seeing the fallback's refusal rather than the guard's confirms the guard stood down in read-write mode and the fallback is what declined. At a confirming configuration that message is unreachable, since `rw_*` refuses the write earlier for want of a prompt.

Everything that does not need the batch kept working under the denial: all four discovery tools, `list_clusters`, and `review_cluster`, whose 11 diagnostic queries all ran and returned 10 findings. Those use server-authored SQL classified as reads, which is why they route through the fallback cleanly.

On the unchanged side, read-only still refuses the deny-list (UNLOAD, VACUUM, SET, RESET, TRUNCATE, BEGIN, ABORT, GRANT, COMMIT inside a transaction), still lets non-enumerated DDL and DML through to the engine backstop where the read-only transaction rejects them, and leaves nothing behind — venue and sales row counts are unchanged and no probe table survives. Named transactions open, carry statements bare on their session, survive a guard rejection, drop on a failed statement, cap at 10 per target while a second target still accepts one, and are bound to their own server process. Under `rwu_*` a rollback discarded its row and a commit persisted one, on both warehouse types.

One gap is structural rather than a defect: `rw_*` cannot complete a confirmation round trip because this CLI does not advertise elicitation, so only the fail-closed branch is observable. The unit suite is green at 388 passed. The uncommitted working-tree changes are confined to the e2e harness (report summary format) and change no server behaviour.

[tool] Running: git status && echo "=== BRANCH ===" && git branch --show-current && echo "=== LOG ===" && git log --oneline -15

[tool] status: Completed

[tool] Running: git diff --stat 6d6067cd..HEAD -- . && echo "=== UNCOMMITTED ===" && git diff --stat

[tool] status: Completed

[tool] Reading sql_guard.py:1, consts.py:1

[tool] status: Completed

[tool] Running: grep -n "no_batch\|NoBatch\|BatchExecuteStatement\|AccessDenied\|REPROBE\|fallback" awslabs/redshift_mcp_server/redshift.py | head -80

[tool] status: Completed

[tool] Reading redshift.py:56-175, redshift.py:471-720

[tool] status: Completed

[tool] Reading redshift.py:826-1015

[tool] status: Completed

[tool] Running: grep -n "_execute_statement\|execute_statement(\|def discover\|enforce_read_only" awslabs/redshift_mcp_server/redshift.py | sed -n '1,80p'; echo "=== executor ==="; grep -n "execute\|enforce" awsla...

[tool] status: Completed

[tool] Reading redshift.py:1401-1510

[tool] status: Completed

[tool] Running: grep -n "might_write\|elicit\|confirm\|access_mode\|read_write\|ToolError" awslabs/redshift_mcp_server/server.py | head -60

[tool] status: Completed

[tool] Reading server.py:229-338

[tool] status: Completed

[tool] Running: grep -n "def test_\|^class " tests/test_redshift.py | sed -n '1,200p'

[tool] status: Completed

[tool] Running: grep -n "def test_\|^class " tests/test_server.py | sed -n '1,200p'

[tool] status: Completed

[tool] Running: grep -n "def test_\|^class " tests/test_sql_guard.py; echo "=== SCENARIOS DIFF ==="; git diff e2e_tests/scenarios.py

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_clusters

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_databases

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_schemas

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_tables

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_columns

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/review_cluster

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: ls -la e2e_tests/.logs/ 2>/dev/null && echo "=== latch warnings per log ===" && for f in e2e_tests/.logs/*; do echo "--- $f: $(grep -c 'BatchExecuteStatement is denied' "$f" 2>/dev/null) latch warn...

[tool] status: Completed

[tool] Running: grep 'BatchExecuteStatement is denied' e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log | cut -c1-30; echo "=== nbw ==="; grep 'BatchExecuteStatement is denied' e2e_tests/.logs/awslabs.reds...

[tool] status: Completed

[tool] Running: awk '$0 >= "2026-09-14 00:46:4"' e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log | grep -n 'SQL:\|denied\|compatibility path' | head -12; echo "=== BEGIN READ ONLY sent on nb after latch? ...

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] Running: grep -c 'BatchExecuteStatement is denied' e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log; grep 'BatchExecuteStatement is denied' e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log |...

[tool] status: Completed

[tool] Running: python3 -c "

import json,glob

for p in glob.glob('e2e_tests/.kiro/agents/*.json'):

    d=json.load(open(p))

    for name,s in d.get('mcpServers',{}).items():

        env={k:v for k,v in s.get('env'...

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: git diff 6d6067cd..HEAD -- awslabs/redshift_mcp_server/review/executor.py

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/review_cluster

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_clusters

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: uv run pytest -q 2>&1 | tail -8

[tool] status: Completed
