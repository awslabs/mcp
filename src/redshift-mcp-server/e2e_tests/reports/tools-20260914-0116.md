# End-to-end test: Every tool, both warehouse types

2026-09-14 01:16 UTC

- **Scenario**: `tools`
- **Code under test**: `feat/redshift-session-redesign` at `0b92cdd4`, uncommitted changes
- **Agent**: `kiro-cli`, model `claude-opus-5`
- **Exit status**: `0`
- **Duration**: 14.0 min
- **Region**: `us-east-1`
- **Provisioned**: `mcp-e2e-provisioned`, ra3.large x2
- **Serverless**: `mcp-e2e-serverless`, 8 RPU
- **Sample data**: `tickit` in `dev`, 424,319 rows each

## Summary

| Scenario | Result | Comment |
|---|---|---|
| `list_clusters` | PASS | Both harness warehouses found and `available`; other clusters in the account listed only, never queried. Works under the denied-batch fallback, which needs no Data API. |
| `list_databases` | PASS | `dev` reported as `local` on both warehouses, alongside auto-mounted catalogues. Also served through the fallback. |
| `list_schemas` | PASS | `tickit` present on both, `schema_type` `local`. Also served through the fallback. |
| `list_tables` | PASS | All 7 TICKIT tables on both warehouses; an unknown schema returns an empty list, not an error. Also served through the fallback. |
| `list_columns` | PASS | All 10 `tickit.sales` columns with types, precision and nullability identical on both; an unknown table returns an empty list. Also served through the fallback. |
| `execute_query` | PASS | Reads agree row-for-row across both warehouses; Data API typing matches the documented contract (int/bigint as integers, double as float, boolean, NULL as null, decimal/date/timestamp/SUPER as strings). Read-only deny-list, single-statement rule, fail-closed parse rejection, no-false-positive cases, engine backstop, failed user SQL, unknown cluster, full named-transaction lifecycle and the breaker all behave; `rw_*` refuses writes fail-closed, `rwu_*` writes and commits/rolls back for real, `nb_*`/`nbw_*` refuse writes and transactions with the fallback's own explanation. |
| `review_cluster` | PASS | 11 signals on the provisioned cluster, 8 on Serverless, where the provisioned-only diagnostics (`NodeDetails`, `WLMConfig`, `CopyPerformance`, `WorkloadEvaluation`) are skipped and `ServerlessScaling` runs instead. Also completes through the fallback, since it issues only reads. |

- `rw_*` never completes a confirmation round trip: `kiro-cli` does not advertise MCP elicitation, so every write there was refused with the message naming `UNSAFE_SKIP_WRITE_CONFIRMATION`. Only the fail-closed branch is observable from this client; confirm and decline remain untested end to end. This is the known limitation recorded in the harness README, not a defect found here.
- Two guard behaviours were not exercised live and rest on unit coverage alone: rejection of SQL longer than `MAX_SQL_LEN` (65,536 characters) and the recursion-limit rejection of deeply nested input. Both are local checks with no service interaction, and sending 64 KB payloads through the transcript was not worth the cost.
- `MAX_OPEN_TRANSACTIONS_PER_TARGET` (cap of 10 per cluster:database) and `SESSION_KEEPALIVE` idle expiry were not exercised; reaching either needs either 11 concurrent transactions or a 10-minute idle wait.
- A `nb_*` review of the Serverless workgroup reported one extra finding, `tables with out of date statistics`, absent from the earlier `ro_*` review of the same workgroup. The cause is my own scratch table, created between the two reviews and not yet analysed; it is not a difference between configurations.
- All writes were confined to `public.mcp_e2e_rwu` on each warehouse, created under `rwu_*` and dropped at the end. `pg_tables` shows no `mcp_e2e%` table remaining on either, and the seeded `tickit` data is unmodified.

## Prompt

Written by the harness, not by hand. The agent derives the cases from the package's unit tests, and is told where to look rather than what to expect.

You have the Redshift MCP server available at several configurations, each running the working tree you are testing. Every configuration exposes the same tools, distinguished by prefix, so `ro_execute_query` and `rw_execute_query` are the same tool at two configurations:
- `ro_*` — default configuration: read-only, which is what a user gets by installing it
- `rw_*` — ACCESS_MODE=read-write, so writes are permitted and each one is confirmed
- `rwu_*` — ACCESS_MODE=read-write with UNSAFE_SKIP_WRITE_CONFIRMATION=true, so writes run unconfirmed
- `nb_*` — default configuration, but its credentials are denied redshift-data:BatchExecuteStatement
- `nbw_*` — ACCESS_MODE=read-write with UNSAFE_SKIP_WRITE_CONFIRMATION=true, and its credentials are denied redshift-data:BatchExecuteStatement

Two warehouses are provisioned and seeded with the TICKIT sample schema `tickit` in database `dev`: provisioned cluster `mcp-e2e-provisioned` and Serverless workgroup `mcp-e2e-serverless`. Both hold the same tables, so a check that holds on one should hold on the other. Other clusters exist in this account; leave them alone.

Run a complete set of end-to-end tests covering all of the server's tools. Check both the provisioned cluster and the Serverless workgroup, including the database schema exploration in both. Check the SQL read-only protection, the transaction breaker protection, and failed user SQL behaviour. Get the test scenario ideas from the unit tests under the project directory.

End your reply with a section that begins on its own line with `## Summary` and holds nothing but,
in this order:

1. A markdown table with the columns Scenario, Result and Comment. One row per tool. Result is
   PASS or FAIL. Leave Comment empty unless there is something to say.
2. Only if anything is worth noting, a short bulleted list under the table.

This section is lifted into a committed report and read on its own, so keep every row
intelligible without the rest of the reply.

## Transcript

I'll start by looking at the unit tests to understand what behaviours to cover.Discovery and typed reads hold on both warehouses. Now the read-only guard.Now a read-only named transaction, opened and closed in one stretch.Transaction survived the guard rejection. Now the breaker itself — a statement that reaches the engine and fails.Now the read-write configuration, where each write must be confirmed.Now the unconfirmed read-write configuration, which actually writes. I'll use a scratch table and drop it afterwards.Writes persist. Now a read-write transaction, rolled back.The breaker discarded the uncommitted write. Same write cycle against Serverless.Now the fallback configurations, whose credentials are denied `BatchExecuteStatement`.Only the table I created intentionally exists. Cleaning up.Testing is complete and the two scratch tables I created were dropped; both warehouses are back to the seeded state.

What the run covered: all seven tools against both `mcp-e2e-provisioned` and `mcp-e2e-serverless`, at all five configurations. Scenario ideas came from `tests/test_sql_guard.py` (deny-list, no-false-positives, fail-closed, engine-backstop classes) and the transaction contract in `redshift.py`.

Notable observations beyond pass/fail:

The `query_id` suffix is a reliable witness to which path served a statement. Read-only mode returns `<batch>:3` (the caller sits third behind `SET application_name` and `BEGIN READ ONLY`, with `ROLLBACK` after), read-write returns `:2` (no wrapper), and the denied-batch fallback returns a bare id with no suffix. That matched the configured mode on every call.

The read-only deny-list and the engine backstop are genuinely separate layers, and both hold. `COMMIT`, `TRUNCATE`, `GRANT`, `VACUUM`, `SET` and `UNLOAD` were refused by the guard before execution, including `TRUNCATE` hidden behind a nested-comment prefix. `CREATE TABLE`, `INSERT`, `SELECT ... INTO` and a `WITH ... UPDATE` data-modifying CTE passed the guard as designed and were refused by the engine with `ERROR: transaction is read-only`. Afterwards `pg_tables` held no `mcp_e2e%` table and `tickit.venue` held no probe row on either warehouse.

The transaction breaker distinguishes correctly between the two ways a statement can fail. A guard rejection inside an open transaction (`SET transaction_read_only TO off` under `in_transaction`) left the transaction usable — the next read on it succeeded — because the guard raises before the session is touched. An engine failure inside one dropped the name, and a later `commit_transaction` reported the transaction gone rather than claiming success. Under `rwu_*` I verified the consequence that matters: a row inserted before the failing statement was absent afterwards, so the breaker's rollback really discarded it.

One correction to my own test, not to the server: my first `INSERT` backstop probe used `venueid = 999999` against a `smallint` column and failed with `ERROR: smallint out of range` before the read-only check could refuse it. Retried in range, it returned `transaction is read-only`.

[tool] Reading listing redshift-mcp-server

[tool] status: Completed

[tool] Reading sql_guard.py:1, test_sql_guard.py:1

[tool] status: Completed

[tool] Reading server.py:1, consts.py:1

[tool] status: Completed

[tool] Running: grep -n "def \|breaker\|Breaker\|no_batch\|NoBatch\|unknown\|already open\|MAX_OPEN\|at most one\|Only one" /local/home/sergkono/AWSLabsMCP/src/redshift-mcp-server/awslabs/redshift_mcp_server/redsh...

[tool] status: Completed

[tool] Reading redshift.py:241-640, redshift.py:831-1030, redshift.py:1341-1520

[tool] status: Completed

[tool] Reading redshift.py:634-833, redshift.py:57-96

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_clusters

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_databases

[tool] Running: @awslabs.redshift-mcp-server/list_databases

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_schemas

[tool] Running: @awslabs.redshift-mcp-server/list_schemas

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_tables

[tool] Running: @awslabs.redshift-mcp-server/list_tables

[tool] Running: @awslabs.redshift-mcp-server/list_tables

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_columns

[tool] Running: @awslabs.redshift-mcp-server/list_columns

[tool] Running: @awslabs.redshift-mcp-server/list_columns

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/review_cluster

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/review_cluster

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_clusters

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_schemas

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_columns

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/review_cluster

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_databases

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_tables

[tool] Running: @awslabs.redshift-mcp-server/execute_query

[tool] status: Completed

[tool] status: Completed

[tool] status: Completed
