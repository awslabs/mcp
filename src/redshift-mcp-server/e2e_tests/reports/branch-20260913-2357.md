# End-to-end test: Changes on this branch

2026-09-13 23:57 UTC

- **Scenario**: `branch`
- **Code under test**: `feat/redshift-session-redesign` at `21d2579e`, uncommitted changes
- **Agent**: `kiro-cli`, model `claude-opus-5`
- **Exit status**: `0`
- **Duration**: 13.7 min
- **Region**: `us-east-1`
- **Provisioned**: `mcp-e2e-provisioned`, ra3.large x2
- **Serverless**: `mcp-e2e-serverless`, 8 RPU
- **Sample data**: `tickit` in `dev`, 424,319 rows each

## Prompt

Written by the harness, not by hand. The agent derives the cases from the package's
unit tests, and is told where to look rather than what to expect.

```text
You have the Redshift MCP server available at several configurations, each running the working tree you are testing. Every configuration exposes the same tools, distinguished by prefix, so `ro_execute_query` and `rw_execute_query` are the same tool at two configurations:
- `ro_*` — default configuration: read-only, which is what a user gets by installing it
- `rw_*` — ACCESS_MODE=read-write, so writes are permitted and each one is confirmed
- `rwu_*` — ACCESS_MODE=read-write with UNSAFE_SKIP_WRITE_CONFIRMATION=true, so writes run unconfirmed
- `nb_*` — default configuration, but its credentials are denied redshift-data:BatchExecuteStatement
- `nbw_*` — ACCESS_MODE=read-write with UNSAFE_SKIP_WRITE_CONFIRMATION=true, and its credentials are denied redshift-data:BatchExecuteStatement

Two warehouses are provisioned and seeded with the TICKIT sample schema `tickit` in database `dev`: provisioned cluster `mcp-e2e-provisioned` and Serverless workgroup `mcp-e2e-serverless`. Both hold the same tables, so a check that holds on one should hold on the other. Other clusters exist in this account; leave them alone.

Run an end-to-end test covering the scenarios for the changes introduced in the current branch only. Scope is the committed branch diff AND the uncommitted working-tree changes, so read `git status` and `git diff` as well as the branch commits: the newest behaviour may not be committed yet. Get the test scenario ideas from the unit tests under the project directory.

Some of the branch behaviour appears only when the AWS principal is denied a specific IAM action, which is what the denied configurations above are for. Work out from the diff and the unit tests what each tool should do under those credentials, and verify it. Verify too that behaviour under the configurations that are not denied it is unchanged.

Provide a short testing summary, one line per scenario.
```

## Transcript

```text
I'll start by understanding what's in the current branch.Now let me confirm the environment is reachable and the denial is actually in effect.Denial is genuine and the fallback served a user-table read. Now the read-only baseline on both warehouses.Now the fallback's refusals — first a write under the denied read-only configuration.Now the read-only configuration's guard and wrapper.Now the wrapper backstop: a write the deny-list does not enumerate.Now named transactions under the read-only configuration.Now the read-write configurations. First `rw_*`, which must ask and cannot.Now `rwu_*`, where writes actually run. Creating a scratch table on the harness clusters only.Now the read-only snapshot, with a concurrent committed write from the other process.Now a read-write transaction: rollback must discard, commit must persist.Now the same write path on the serverless workgroup.Same transaction name against two different targets should be independent.Last denied call was inside the 300s re-probe window. It has now elapsed, so the next call should probe the batch path again.All scenarios ran against both warehouses. Scratch tables dropped, seeded data intact.

## Scope

The branch (7 commits off `origin/main`) covers: opt-in read-write mode with per-write confirmation, every statement sent as one Data API batch, named transactions on transaction-scoped sessions, anticipated failures surfaced to the model, and the `no_batch` fallback for credentials denied `redshift-data:BatchExecuteStatement`. The uncommitted working-tree changes are the harness only — gitignore entries, a `tomli` dev dependency, and the untracked `e2e_tests/` directory — so no product behaviour was uncommitted.

## Results

| Scenario | Result |
|---|---|
| Cluster discovery, both warehouse types | Pass — 6 clusters, both harness warehouses `available` |
| Metadata discovery (`list_databases/schemas/tables/columns`) via the batch path | Pass — 7 tickit tables, venue columns, 4 databases |
| Read returning every value type | Pass — ints/bigints as integers, real/double as floats, bool, null, decimal/date/timestamp as strings |
| Same read on provisioned and serverless | Pass — identical (172456 sales, 345349 qty, 642.28 avg) |
| Read-only deny-list before execution | Pass — GRANT, SET, TRUNCATE each rejected naming the keyword |
| Multi-statement rejection, both modes | Pass — rejected under `ro_` and `rwu_` |
| Unparseable SQL fails closed | Pass — "SQL could not be parsed" |
| Deny-listed words as identifiers/aliases/literals | Pass — no false positives (`AS grant`, `'vacuum'`, `AS "set"`) |
| `BEGIN READ ONLY` wrapper as the write backstop | Pass — CREATE TABLE and INSERT both "ERROR: transaction is read-only"; neither persisted |
| Engine failure detail reaches the model | Pass — `relation "tickit.no_such_table" does not exist` |
| Read-only named transaction across calls | Pass — one shared connection (`application_name` persisted), snapshot held at 1 row while another process committed a 2nd, 2 rows after close |
| Guard inside a transaction leaves it open | Pass — COMMIT-as-SQL rejected, next statement on the name succeeded |
| Duplicate name, unknown name, blank name, two parameters, missing `sql` | Pass — each refused with its own message |
| Failed statement aborts and drops the name | Pass — next call reports the name unknown, not a silent no-op commit |
| Transaction keyed per cluster+database, and per process | Pass — same name open on both warehouses independently; `ro_` cannot see `rwu_`'s name |
| `rw_` write with no elicitation capability | Pass — fail-closed, error names `UNSAFE_SKIP_WRITE_CONFIRMATION`; happened outside and inside a transaction |
| `rw_` reads and transaction close ask nothing | Pass — read ran unprompted, bare commit succeeded after a declined write left the transaction open |
| `rwu_` writes execute and persist | Pass — CREATE/INSERT/DROP; row read back from a different server process |
| `rwu_` transaction rollback vs commit | Pass — rolled-back row absent, committed row present; commit-with-last-statement also persisted |
| `rwu_` deny-list not applied | Pass — `SET query_group` accepted |
| `review_cluster`, provisioned and serverless | Pass — 11 signals/10 findings provisioned; 8 signals serverless with provisioned-only ones skipped and `ServerlessScaling` added |
| **Denied batch:** reads keep working | Pass — user-table aggregate and all four discovery tools return the same rows as `ro_` |
| **Denied batch:** `review_cluster` | Pass — full 11-signal review through the fallback |
| **Denied batch:** write refused, read-only config (`nb_`) | Pass — refusal names `redshift-data:BatchExecuteStatement`; no table created |
| **Denied batch:** write refused, confirmation skipped (`nbw_`) | Pass — same grant-naming refusal, not a confirmation refusal, which is the gate `nbw_` exists to reach |
| **Denied batch:** named transactions refused | Pass — distinct transaction-specific message under both `nb_` and `nbw_` |
| **Denied batch:** latch and re-probe | Pass — one warning per window (2 latches, 661s apart, across 16 statements), zero batch calls while latched, and the re-probing call still returned its rows |
| Non-denied configurations unchanged | Pass — logs show 0 latches and 0 fallback statements across `ro_`/`rw_`/`rwu_`; all 48 statements took the batch path |

Two observable signatures confirm the routing rather than just the outcome: the batch path returns a sub-statement `query_id` (`…:3` read-only, `…:2` read-write, `…:1` inside a transaction), while the fallback returns a bare statement id. The denial itself is a real explicit deny in a session policy on `arn:…/mcp-e2e-denied-batch`, quoted in the latch warning.

Not reachable from this client: a completed confirm or decline round trip in `rw_` (the CLI does not advertise elicitation, so only the fail-closed branch is observable), the per-mode tool annotations, and the `MAX_OPEN_TRANSACTIONS_PER_TARGET` cap — all covered by unit tests only.

[tool] Running: git status && echo "=== BRANCH ===" && git branch --show-current && echo "=== LOG ===" && git log --oneline -20
[tool] status: Completed

[tool] Running: git log --oneline origin/main -3 2>/dev/null; echo "=== MERGE BASE ==="; git merge-base HEAD origin/main 2>/dev/null || git merge-base HEAD main
[tool] status: Completed

[tool] Running: git diff --stat c9102a9a..HEAD -- . ; echo "=== UNCOMMITTED (tracked) ==="; git diff -- . ../../.gitignore
[tool] status: Completed

[tool] Reading consts.py:1, sql_guard.py:1
[tool] status: Completed

[tool] Running: wc -l awslabs/redshift_mcp_server/*.py tests/*.py
[tool] status: Completed

[tool] Reading redshift.py:1
[tool] status: Completed

[tool] Reading server.py:1
[tool] status: Completed

[tool] Running: grep -n "def test_\|^class \|# ---" tests/test_redshift.py
[tool] status: Completed

[tool] Reading test_redshift.py:2369-2688
[tool] status: Completed

[tool] Running: grep -n "def test_\|^class " tests/test_server.py tests/test_sql_guard.py
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_clusters
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query
[tool] status: Completed

[tool] Running: ls -la e2e_tests/.logs/ 2>/dev/null && echo "=== nb log tail ===" && tail -20 e2e_tests/.logs/*no-batch*.log 2>/dev/null
[tool] status: Completed

[tool] Reading awslabs.redshift-mcp-server-no-batch.log:1
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/list_tables
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_tables
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_columns
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/list_databases
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/review_cluster
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: ls e2e_tests/.kiro/agents/ && grep -o '"[A-Z_]*": *"[^"]*"' e2e_tests/.kiro/agents/*.json | sort -u | head -40
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/review_cluster
[tool] status: Completed

[tool] Running: grep -c "_latch_no_batch" e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log; echo "--- latch timestamps ---"; grep "_latch_no_batch" e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log ...
[tool] status: Completed

[tool] Running: grep -E "compatibility path|_latch_no_batch|WARNING" e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log | cut -c1-19,80-140 | tail -25; echo "=== now ==="; date -u '+%Y-%m-%d %H:%M:%S'
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-no-batch/execute_query
[tool] status: Completed

[tool] Running: grep "_latch_no_batch" e2e_tests/.logs/awslabs.redshift-mcp-server-no-batch.log | cut -c1-19 ; echo "--- total latches ---"; grep -c "_latch_no_batch" e2e_tests/.logs/awslabs.redshift-mcp-server-no...
[tool] status: Completed

[tool] Running: for f in awslabs.redshift-mcp-server awslabs.redshift-mcp-server-read-write awslabs.redshift-mcp-server-read-write-unsafe; do echo "=== $f: latches=$(grep -c _latch_no_batch e2e_tests/.logs/$f.log)...
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server-read-write-unsafe/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed

[tool] Running: @awslabs.redshift-mcp-server/execute_query
[tool] status: Completed
```
