apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: eks-mcp-server

resources:
- configmap.yaml
- deployment.yaml
- service.yaml

commonLabels:
  app.kubernetes.io/name: eks-mcp-server
  app.kubernetes.io/component: mcp-server
  app.kubernetes.io/part-of: aws-mcp-servers

# Uncomment to add namespace
# namespace: mcp-servers

# Example patches for customization
# patchesStrategicMerge:
# - deployment-patch.yaml

# Example for setting image tag
# images:
# - name: public.ecr.aws/awslabs/mcp/eks-mcp-server
#   newTag: v1.0.0

